syntax = "proto3";

package auth;

option go_package = ".;authpb";

service AuthService {
  rpc GetPublicKey (Empty) returns (PublicKeyResponse);
}

message Empty {}

message PublicKeyResponse {
  string pem = 1;
}