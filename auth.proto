syntax = "proto3";

package auth;

option go_package = ".;proto";

service AuthService {
  rpc GetPublicKey (Empty) returns (PublicKeyResponse);
}

message Empty {}

message PublicKeyResponse {
  string pem = 1;
}