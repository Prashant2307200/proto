syntax = "proto3";

package auth;

option go_package = "github.com/Prashant2307200/proto/gen;authpb";

service AuthService {
  rpc GetPublicKey (Empty) returns (PublicKeyResponse);
}

message Empty {}

message PublicKeyResponse {
  string pem = 1;
}